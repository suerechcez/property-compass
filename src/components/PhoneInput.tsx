// PhoneInput has been removed. Phone fields across the app now use a plain
// <Input type="tel"> without country-code highlights or flag selectors.
// This file is intentionally left as a stub to avoid a 404 if any cached
// import still references it — it exports nothing.
export {};
